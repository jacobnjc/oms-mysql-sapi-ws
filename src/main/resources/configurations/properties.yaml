api:
  groupId:        "${api.groupId}"
  artifactId:     "${api.artifactId}"
  version:        "${api.version}"
  spec:           "resource::${api.groupId}:${api.artifactId}:${api.version}:raml:zip:oms-mysql-sapi.raml"
  majorVersion:   "v1"
  apiId:          "20591876"

https:
  port: "8081"

db: 
  driver_class: "org.sqlite.JDBC"
  url: 'jdbc:sqlite:C:/Users/NJC Labs/AnypointStudio/Centimark_POCs/oms-mysql-sapi/src/main/resources/oms-database.db'
  test:  'jdbc:sqlite:${mule.home}/apps/${app.name}/oms-database.db'

tls.keystore:
    path: "certs/oms-mysql-sapi.p12"
    type: "pkcs12"
    alias: "centi-server"